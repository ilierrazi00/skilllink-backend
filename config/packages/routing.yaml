framework:
    router:
        # Force la génération correcte des URLs absolues
        default_uri: 'http://127.0.0.1:8000'

when@prod:
    framework:
        router:
            strict_requirements: null